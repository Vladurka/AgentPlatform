{
  "name": "@agentforge/widget",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/widget.ts --bundle --minify --outfile=dist/widget.js --platform=browser --target=es2017",
    "build:dev": "esbuild src/widget.ts --bundle --outfile=dist/widget.js --platform=browser --target=es2017 --sourcemap"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
